#include <ctype.h>
#include <string.h>
#include <stdio.h>

// 0x400
// 0x800

int main(int argc, char **argv, char **envp) {

    int type;

    type = isalpha('a');

    /* for(i = 0; i < strlen(a); ++i) { */
    /*     type = isalpha(a[i]); */
    /*     printf("%d\n", type); */
    /* } */
}
